export default class Hero {
  constructor(name, role, attackType, url, icon) {
    this.name = name;
    this.role = role;
    this.attackType = attackType;
    this.url = url;
    this.icon = icon;
  }
}
